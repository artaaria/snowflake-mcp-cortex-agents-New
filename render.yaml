services:
  - type: web
    name: mcp-streamlit-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: MISTRAL_API_KEY
        value: UtJt7V0in6nZofPtgSWXwXLnmzLZNbyA
